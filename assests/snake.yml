name: Generate contribution snake

on:
  schedule:
    - cron: "0 */12 * * *"   # a cada 12h
  workflow_dispatch:          # botão "Run workflow" manual
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  snake:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=#6e01d8&color_dots=#ebedf0,#e9d5ff,#c084fc,#9d4dff,#6e01d8
            dist/github-snake-dark.svg?palette=github-dark&color_snake=#c026d3&color_dots=#161b22,#3b0764,#6b21a8,#9d4dff,#e9d5ff

      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
